json.extract! contract, :id, :numero, :fecha, :descripcion, :obac, :postor, :proveedor, :moneda, :adjudicado, :presupuestado, :admin_user_id, :created_at, :updated_at
json.url contract_url(contract, format: :json)
